<%# 설비 상태 섹션 %>
<%# 각 설비의 가동 상태를 실시간으로 표시합니다 %>
<%# @equipments: 컨트롤러에서 전달받은 설비 데이터 배열 %>
<div class="bg-white rounded-2xl shadow-sm border border-slate-200 overflow-hidden">
  <div class="px-6 py-4 border-b border-slate-100">
    <h3 class="text-lg font-semibold text-slate-800">설비 상태</h3>
    <p class="text-sm text-slate-500">실시간 설비 모니터링</p>
  </div>
  <div class="p-6">
    <div class="grid grid-cols-2 gap-3">
      <% @equipments.each do |equipment| %>
        <%= render "dashboard/equipment_item", equipment: equipment %>
      <% end %>
    </div>
    <%# 설비 상태 범례 %>
    <div class="mt-4 pt-4 border-t border-slate-100 flex items-center justify-center space-x-4 text-xs text-slate-500">
      <div class="flex items-center"><span class="w-2 h-2 bg-emerald-500 rounded-full mr-1.5"></span>가동</div>
      <div class="flex items-center"><span class="w-2 h-2 bg-slate-400 rounded-full mr-1.5"></span>대기</div>
      <div class="flex items-center"><span class="w-2 h-2 bg-amber-500 rounded-full mr-1.5"></span>PM</div>
      <div class="flex items-center"><span class="w-2 h-2 bg-gnt-red rounded-full mr-1.5"></span>고장</div>
    </div>
  </div>
</div>
